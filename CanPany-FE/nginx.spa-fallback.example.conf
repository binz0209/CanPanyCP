# Ví dụ: gắn vào server {} của Nginx khi phục vụ thư mục dist (hoặc proxy tới static).
# Mọi route không trùng file thật đều trả index.html để React Router xử lý.

location / {
  try_files $uri $uri/ /index.html;
}
